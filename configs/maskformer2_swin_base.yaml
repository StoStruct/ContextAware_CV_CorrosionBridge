_BASE_: maskformer2_R50_bs16_50ep.yaml
MODEL:

  WEIGHTS: "models/maskformer2_swin_base_bs16_50ep.pkl"

  BACKBONE:
    NAME: "D2SwinTransformer"

  SEM_SEG_HEAD:
    NUM_CLASSES: 3

  SWIN:
    EMBED_DIM: 128
    DEPTHS: [2, 2, 18, 2]
    NUM_HEADS: [4, 8, 16, 32]
    WINDOW_SIZE: 12
    APE: False
    # CRITICAL FIX: Reduced drop path rate for better stability
    DROP_PATH_RATE: 0.1      # Reduced from 0.3 to prevent overfitting
    PATCH_NORM: True
    OUT_FEATURES: ["res2", "res3", "res4", "res5"]
    # CRITICAL FIX: Match pretrained weight dimensions
    PRETRAIN_IMG_SIZE: 384   # Must match pretrained weights

  PIXEL_MEAN: [123.675, 116.280, 103.530]
  PIXEL_STD: [58.395, 57.120, 57.375]
